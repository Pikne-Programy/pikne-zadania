/* eslint-disable no-unused-vars, @typescript-eslint/no-unused-vars */
import { createRange, createUnknown, createVariable } from './highlight.utils';

//FIXME Enable linter rules
/* eslint-disable jasmine/no-spec-dupes, jasmine/missing-expect */
describe('Exercise editor Highlighting Utils', () => {
    xdescribe('createVariable', () => {
        it('should ...', () => {});
    });

    xdescribe('createRange', () => {
        it('should ...', () => {});
    });

    xdescribe('createUnknown', () => {
        it('should ...', () => {});
    });
});
