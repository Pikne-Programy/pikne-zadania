/* tslint:disable:no-unused-variable */

import { SafeHtmlPipe } from './safe-html.pipe';

xdescribe('Pipe: SafeHtml', () => {
  it('create an instance', () => {
    //let pipe = new SafeHtmlPipe();
    //expect(pipe).toBeTruthy();
  });
});
